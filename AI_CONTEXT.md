# AI_CONTEXT.md - 프로젝트 기술적 맥락 및 다음 세션 인수인계 가이드

본 문서는 에이전트가 프롬프트 캐싱(Prompt Caching) 히트율을 극대화하고 다음 세션에서 작업을 즉시 이어받을 수 있도록 작성된 프로젝트 마스터 파일입니다.

---

## 1. 현재 Git 브랜치 및 최신 커밋 상태
- **작업 브랜치**: `feat/translate-318-indexed-posts` (main push 절대 금지)
- **보유 번역 산출물 저장 위치**: [`src/data/pending-translations/`](file:///C:/Users/natey/Desktop/wisdom-giants/src/data/pending-translations/) (Git 추적 상태)
  - `pl_44_pending.json` (폴란드어 44건)
  - `uk_39_pending.json` (우크라이나어 39건)
  - `temp_12_pending.json` (기타 12건)
  - **총 95건 완수 산출물 보존 완료 (재번역 0회 필요)**

---

## 2. 블로그 다국어 번역 현황 요약
- **1차 색인 언어 미번역 대상**: 총 318건 (pl 124, uk 116, id 20, de 10, ha 10, he 10, sw 10, ru 9, ja 8, el 1)
- **현재 생성 산출물 현황**: 95건 보존 완료 (실제 `blog-posts.ts` 병합 완료: 0건 - 품질 게이트 오탐 수정 전 안전 보류)
- **잔여 번역 대상**: 313건 (95건 구제 병합 후 218건 신규 번역 필요)
- **보류 중인 신규 포스트 20편**: 작업 계속 보류 중 (195편 원래 데이터베이스 유지)

---

## 3. 품질 게이트 3종 오탐 원인 및 수정 방안 (다음 세션 1단계 필수)

### (1) 영단어 정규식 `/[a-zA-Z]/` 오탐
- **원인**: 폴란드어(`pl`), 독일어(`de`), 인도네시아어(`id`), 하우사어(`ha`), 스왈리어(`sw`) 등 라틴/로만 알파벳을 사용하는 언어의 디아크리틱 없는 정상 문장이 "8연속 영단어"로 잘못 판정되어 차단됨. (대표님 검수 승인 파일럿 1번 `fear-giants`도 오탐 차단됨 확인)
- **수정 방안**: 라틴 문자권 언어에서는 `/[a-zA-Z]/` 검사를 해제하고, 키릴(`uk`, `ru`), 히브리어(`he`), 가나(`ja`), 그리스어(`el`) 등 **비라틴 문자권에만 적용**. 라틴 문자권은 라틴 문장 비율(>=90%) 및 원문 대조로 판정.

### (2) 배치 단위 거부 $\rightarrow$ 건 단위 판정으로 변경
- **원인**: 40편 배치 중 1편만 오탐 걸려도 40편 전체가 거부되는 설계 결함.
- **수정 방안**: 포스트 **1편 단위 독립 검사**로 전환하여 통과한 포스트는 즉시 병합하고 걸린 포스트만 별도 분류.

### (3) Content length < 50% 오판정 원인 규명
- **원인**: 대표님이 수동 검수 승인해주신 폴란드어 파일럿 글 3편이 Content length < 50%로 걸림. Concise한 번역 스타일이 영어 원문의 긴 포맷/메타데이터 길이와 비교될 때 오판정 발생.
- **수정 방안**: 30% 미만으로 하한선 완화 및 원문 토큰/단어 수 대조 기준 정제.

> [!IMPORTANT]
> **수정 완료 기준선**: 품질 게이트 수정 후, 검수 승인된 파일럿 5편이 게이트를 **100% 통과**하는지 반드시 먼저 검증해야 합니다.

---

## 4. 다음 세션 착수 순서 (Step-by-Step)
1. **1단계**: 품질 게이트 3종 오탐 수정 및 파일럿 5편 100% 통과 실측 확인.
2. **2단계**: `src/data/pending-translations/` 내 보존된 95건 산출물 건 단위 게이트 통과 후 `blog-posts.ts` 병합.
3. **3단계**: 잔여 218건 5~10편 소단위 릴레이 번역 실행.
4. **4단계**: Vercel Preview 배포에서 HTTP GET 실측 검증 (색인 16개 언어 미번역 0건, `blog.xml` = 3,136, 전체 사이트맵 = 14,920개 확인).
