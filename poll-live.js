const https = require('https'); const langs = ['el', 'ja', 'th']; let attempts = 0; function check() { let done = 0; langs.forEach(lang => { https.get('https://www.giantswisdom.com/' + lang, (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => { const match = data.match(/<meta[^>]*name="robots"[^>]*content="([^"]+)"/i); const content = match ? match[1] : 'not found'; if (content === 'index, follow') done++; if (done === 3) { console.log('All three languages are now index, follow!'); process.exit(0); } }); }); }); setTimeout(() => { attempts++; if (attempts < 15) check(); else console.log('Timeout waiting for deployment'); }, 10000); } check();
