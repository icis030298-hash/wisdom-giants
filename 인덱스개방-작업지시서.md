# 블로그 다국어 인덱스 개방 작업 지시서

> 대상 파일: `src/config/locale-status.ts`
> 목표: 번역 완료된 7개 언어(he, el, ha, sw, uk, pl, id)의 블로그를 검색엔진에 개방
> 전제: 7개 언어 × 120개 포스트 title/content/description 번역 완료 및 검증 통과 (2026-07-26 확인)

---

## ⚠️ 함정 — 배열에 7개 추가만 하면 안 됨

`isBlogLocaleIndexed()` 는 **두 조건을 모두** 만족해야 true를 반환합니다.

```ts
export const isBlogLocaleIndexed = (locale: string) => {
  return INDEXED_BLOG_LOCALES.includes(locale) && isLocaleIndexed(locale);
};
```

현재 `LOCALE_STATUS` 에서 **`uk: { index: false }`** 입니다.
따라서 `INDEXED_BLOG_LOCALES` 에 `'uk'` 를 추가해도 **우크라이나어는 열리지 않습니다.**
`LOCALE_STATUS.uk.index` 를 `true` 로 함께 바꿔야 합니다.

나머지 6개(he, el, ha, sw, pl, id)는 이미 `index: true` 이므로 배열 추가만 하면 됩니다.

---

## 1. 수정 내용

### 1-1. `LOCALE_STATUS` — uk만 변경

```ts
  uk: { index: false },   // ❌ 변경 전
  uk: { index: true },    // ✅ 변경 후
```

**uk 외 다른 항목은 절대 건드리지 말 것.**

### 1-2. `INDEXED_BLOG_LOCALES` — 7개 추가 (9 → 16)

```ts
// 변경 전
export const INDEXED_BLOG_LOCALES = ['ko', 'en', 'de', 'es', 'fr', 'it', 'pt', 'ja', 'ru'];

// 변경 후
export const INDEXED_BLOG_LOCALES = [
  'ko', 'en', 'de', 'es', 'fr', 'it', 'pt', 'ja', 'ru',
  'he', 'el', 'ha', 'sw', 'uk', 'pl', 'id',
];
```

---

## 2. 절대 추가하면 안 되는 언어 (8개)

아래 8개는 **description만 번역돼 있고 title/content는 영어 그대로**일 가능성이 높습니다.
검증되지 않았으므로 **이번 개방 대상이 아닙니다.**

```
ar, zh, nl, hi, th, tr, vi, fa
```

이 언어들은 `LOCALE_STATUS` 에서 `index: true` 라서 일반 페이지는 이미 노출되지만,
`INDEXED_BLOG_LOCALES` 에 없으므로 **블로그는 noindex 유지**됩니다. 그대로 두면 됩니다.

> 이 8개를 열려면 먼저 title/content 번역 + 70% 기준 검증을 통과해야 합니다.
> 미번역 상태로 열면 중복/저품질 콘텐츠로 판정되어 **전체 도메인 SEO에 악영향**을 줍니다.

---

## 3. 개방 전 선행 처리 (권장)

7개 언어 사용자가 새로 유입되기 시작하므로, 아래 2건은 개방 전에 고치는 게 좋습니다.

### 3-1. 404 페이지 다국어 미적용 (중요)
현재 `/ko/` 등 모든 로케일에서 없는 페이지 접근 시 **"404 Page Not Found"가 영어로만** 표시됩니다.
7개 언어를 열면 해당 언어 사용자가 404를 만났을 때 전부 영어를 보게 됩니다.
→ 404 페이지 문구를 `messages/*.json` 기반으로 다국어화할 것.

### 3-2. 소개 페이지 숫자 불일치
소개 페이지 본문에 *"…등 **101명**의 역사적 거인들"* 이라고 적혀 있으나 실제는 **493명**입니다.
→ 493으로 수정. (24개 언어 전부 해당 문구 확인 필요)

---

## 4. 검증 절차 (수정 후 반드시 실행)

### 4-1. 로직 검증
```
isBlogLocaleIndexed('uk')  → true 여야 함  ← 가장 중요
isBlogLocaleIndexed('he')  → true
isBlogLocaleIndexed('ha')  → true
isBlogLocaleIndexed('ar')  → false 여야 함  ← 안 열려야 정상
isBlogLocaleIndexed('zh')  → false
```

### 4-2. 사이트맵 검증
`src/app/sitemap/[id]/route.ts` 가 `INDEXED_BLOG_LOCALES` 를 사용합니다.

- 로컬에서 사이트맵 생성 후 **블로그 URL 총 개수**를 셀 것
- 기대값: **16개 언어 × 120개 포스트 = 1,920개** (+ 블로그 목록 페이지 16개)
- 변경 전 대비 증가분: 9 → 16 언어이므로 **840개 증가**

### 4-3. 실제 페이지 메타태그 확인
로컬 서버에서 아래 URL의 `<meta name="robots">` 를 확인:

| URL | 기대값 |
|---|---|
| `/uk/blog` | `index, follow` |
| `/he/blog` | `index, follow` |
| `/ha/blog` | `index, follow` |
| `/ar/blog` | `noindex` |
| `/zh/blog` | `noindex` |

### 4-4. 빌드
`npm run build` 통과 확인 후 커밋.

커밋 메시지: `feat(seo): open blog index for 7 verified locales (he, el, ha, sw, uk, pl, id)`

---

## 5. 배포 후 (대표님 직접 진행)

1. **Google Search Console** 에서 사이트맵 재제출
2. 신규 7개 언어 URL 몇 개를 **URL 검사 → 색인 생성 요청**
3. 1~2주 후 색인 현황 및 노출 추이 확인

---

## 6. 하지 말 것

- ❌ `LOCALE_STATUS` 에서 uk 외 다른 항목 변경
- ❌ ar, zh, nl, hi, th, tr, vi, fa 를 `INDEXED_BLOG_LOCALES` 에 추가
- ❌ `buildSEOAlternates` 의 `regionMap` 임의 수정
  (신규 언어는 2자리 언어 코드로 fallback 되며, 이는 유효한 hreflang입니다)
- ❌ 검증 미완료 상태로 커밋/푸시
