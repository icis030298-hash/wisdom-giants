# 블로그 다국어 번역 작업 지시서 (v2 — 문단 단위 방식)

> 대상 파일: `src/data/blog-posts.ts`
> 작업 범위: 각 포스트의 `title`, `content` (`description`은 7개 언어 완료 — 건드리지 말 것)
> 전체 포스트 수: 120개

---

## 0. 번역 수단 제약 (필수)

- ✅ **안티그래비티 자체 LLM 할당량(서브에이전트)만 사용**
- ❌ 외부 번역 API 사용 금지 — `deep-translator`, Google Translate API, DeepL 등 **일체 금지**
- ❌ 번역을 API로 대체하자는 제안 금지. LLM 방식으로 기준을 통과시키는 것이 과제임

---

## 1. 현재 상태 (실측 기준)

| 언어 | description | title/content | 상태 |
|---|---|---|---|
| he (히브리어) | ✅ | ✅ 120/120 검증 통과 | 완료 |
| el (그리스어) | ✅ | 완료 보고 (**미검증**) | 검증 필요 |
| **ha (하우사어)** | ✅ | ❌ **실패 — 재작업 필요** | sw와 동일 증상 |
| **sw (스와힐리어)** | ✅ | ❌ 실패 (잘림 37건, 미번역 48건) | **재작업 필요** |
| uk (우크라이나어) | ✅ | ❌ 85개 미번역 | 대기 |
| pl (폴란드어) | ✅ | ❌ 18개 미번역 | 대기 |
| id (인도네시아어) | ✅ | ❌ 9개 미번역 | 대기 |

---

## 2. 실패 원인과 해결 방향

**증상:** sw 85건 중 잘림 37건, 미번역 48건 (영어 그대로 16건 + langdetect=en 32건). ha도 동일.

**원인:** 작업 단위가 너무 큼. 85개를 8그룹으로 나누면 에이전트 1기가 4000자 장문을 11개씩 처리 →
모델이 후반부에서 맥락을 잃고 영어를 그대로 출력하거나 중간에 끊음. 저자원 언어일수록 심함.

**해결:** 포스트 통째로 번역시키지 말고 **문단 단위로 분해 → 1:1 번역 → 프로그램으로 재조립**.
번역 단위가 작아지면 모델이 드리프트하지 않음. 이것이 LLM 할당량만으로 기준을 통과하는 방법임.

---

## 3. 필수 작업 방식 — 문단 단위 파이프라인

### 3-1. 분해
- 각 포스트의 en `content`를 `\n\n` 기준으로 문단 배열로 분해
- 문단 개수 `N`을 기록 (재조립 시 검증용)
- `### 소제목` 같은 마크다운 헤더도 개별 문단으로 취급
- 맨 끝 `[링크텍스트](/giant/slug)` 형태의 CTA 줄은 **링크 URL을 절대 변경 금지**, 텍스트만 번역

### 3-2. 번역 (서브에이전트 호출 단위 = 문단 1개)
- 서브에이전트 1회 호출 = **문단 1개**. 여러 문단을 한 번에 넘기지 말 것
- 프롬프트에 반드시 포함할 것:
  - 목표 언어 명시 (예: "Translate into Swahili")
  - "출력은 번역문만. 설명·머리말·따옴표 감싸기 금지"
  - "영어를 그대로 반환하지 말 것. 번역이 불가능한 고유명사만 원문 유지"
  - "원문의 마크다운 서식(`###`, `**`, 숫자 목록)을 그대로 유지"
- title은 별도 1회 호출로 처리

### 3-3. 문단 단위 즉시 검증 (핵심)
번역된 문단을 받는 즉시, 재조립 **전에** 아래를 검사하고 실패 시 그 문단만 재호출:
- 반환값이 원문 영어와 동일 → 실패
- 반환 길이가 원문의 50% 미만 → 실패 (잘림)
- langdetect 결과가 `en` → 실패
- 최대 3회까지 재시도, 3회 실패 시 해당 문단을 로그에 남기고 보고

### 3-4. 재조립
- 번역된 문단을 원래 순서대로 `\n\n`으로 재결합
- **문단 개수가 원본 N과 일치하는지 확인** (불일치 시 해당 포스트 실패 처리)
- 재조립된 content를 파일에 주입

---

## 4. 인코딩 규칙 (과거 사고 이력)

히브리어 작업 시 서브에이전트가 cp1252/이중 인코딩을 써서 **8개 포스트가 모지바케로 깨진 사고** 발생.

- 모든 파일 입출력에 **`encoding='utf-8'` 명시 강제** (Python `open()`, Node `fs` 전부)
- 중간 단계(JSON 덤프, 정규식 치환, 임시 파일 경유)에 인코딩 변환이 끼지 않는지 점검
- 주입은 백틱(`) 방식 사용, content 내 백틱·`${` 는 반드시 이스케이프

### ⚠️ 검증 수단이 아닌 것
- **`npm run build` 통과 = 무의미** — 깨진 글자도 JS 문자열로 유효해서 빌드는 통과함
- **글자 수 비교 = 무의미** — 모지바케는 글자 수가 정상이라 길이 체크를 통과함

---

## 5. 사후 검증 기준 (`verify_lang.py`)

주입 완료 후 전수 검사. **하나라도 실패하면 커밋 금지.**

### 공통
| 항목 | 기준 |
|---|---|
| 잘림 | content 길이가 en 대비 **70% 이상** |
| 모지바케 | CJK(U+4E00–9FFF) 비율 10% 미만, 물음표 비율 15% 미만 |
| 미번역 | en 원문과 동일하지 않을 것 |

> 예외: 공자 관련 포스트 영어 원문에 한자 `仁` 1글자가 정상 포함됨. 한자 1~2글자 단독은 허용, 연속 한자+물음표 혼재 패턴만 모지바케로 판정.

### 비라틴 문자 — 유니코드 범위
| 언어 | 범위 | 기준 |
|---|---|---|
| uk | 키릴 U+0400–04FF | 40% 이상 |
| el | 그리스 U+0370–03FF | 40% 이상 |
| he | 히브리 U+0590–05FF | 완료됨 |

### 라틴 문자 (ha, sw, pl, id) — 유니코드 검증 불가, 3종 병행
1. **en 동일성** — en content와 같거나 90% 이상 유사 → 미번역
2. **영어 시작어 패턴** — `Explore`, `Discover`, `Learn`, `Uncover`, `Delve`, `In the late 19th century` 등으로 시작 → 미번역 의심
3. **langdetect** — content 앞 300자 판정이 `en` → 미번역

---

## 6. 진행 순서

```
sw (재작업) → ha (재작업) → uk (85) → pl (18) → id (9) → el (사후 검증)
```

- **한 번에 한 언어씩.** 100% 통과 → 커밋 → 보고 → 다음 언어
- 커밋 분리. 형식: `feat(blog): translate title/content for sw`
- 여러 언어 동시 진행 금지

---

## 7. 보고 형식

언어 하나 끝날 때마다 아래 형식. "다 됐습니다" 식 뭉뚱그린 보고 금지.

```
[sw 완료 보고]
- 번역 주입: 85/85
- 문단 재조립 검증: 문단 수 불일치 0건
- 잘림: X건 (기준 EN 대비 70%) → 통과/실패
- 모지바케: CJK 최대 X.X%, 물음표 최대 X.X% → 통과/실패
- 미번역 잔여: X건 (en 동일 X건, langdetect=en X건) → 통과/실패
- 3회 재시도 후에도 실패한 문단: X건 (목록 첨부)
- 커밋: <해시>
```

**검증 미통과 상태로 커밋하지 말 것.** 실패 건이 남으면 커밋하지 말고 그대로 보고할 것.

---

## 8. 하지 말 것

- ❌ 외부 번역 API 사용 (`deep-translator` 등)
- ❌ `src/config/locale-status.ts` 의 `INDEXED_BLOG_LOCALES` 수정 — 전 언어 완료·검증 후 별도 지시
- ❌ `description` 필드 수정 (7개 언어 840건 완료·검증됨)
- ❌ 여러 언어 동시 진행 / 커밋 통합
- ❌ `--force` 푸시 (과거 알 가잘리 복구 커밋 유실 사고 이력)
- ❌ 검증 실패 상태에서 "완료" 보고

---

## 9. 로컬 검증 서버

GitHub 용량 문제로 푸시 검증이 어려움. 로컬 dev 서버로 검증.

- 언어 작업 완료 후 `npm run dev` 서버를 켜둘 것
- **서버가 최신 코드를 물고 있는지 반드시 확인** — 과거 dev 서버가 옛날 코드를 서빙해 검증이 무의미해진 사례 있음
- 재시작 시: `rm -rf .next && npm run dev`
- 확인 URL 예: `http://localhost:3000/sw/blog`, `http://localhost:3000/sw/blog/seneca-time`
- 서버 구동 후 **본인이 먼저** 해당 URL이 목표 언어로 나오는지 확인하고 보고할 것

---

## 10. 이후 예정

1. sw → ha 재작업, uk/pl/id 신규 번역, el 사후 검증
2. 전 언어 검증 통과 후 `INDEXED_BLOG_LOCALES` 개방
3. 디자인 개선 작업 (별도 지시 예정)
